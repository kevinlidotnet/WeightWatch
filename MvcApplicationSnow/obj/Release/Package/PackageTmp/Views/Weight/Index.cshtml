@model IEnumerable<MvcApplicationSnow.Models.Weight>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FieldA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FieldB)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FieldA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FieldB)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WeightID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WeightID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WeightID })
        </td>
    </tr>
}

</table>
